<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kk.qisheng">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleViewActivity" />
        <activity android:name=".Loading1Activity" />
        <activity android:name=".Loading3Activity" />
        <activity android:name=".DrawPathActivity" />
        <activity android:name=".Loading4Activity" />
        <activity android:name=".Loading6Activity" />
        <activity android:name=".GitTestActivity"></activity>
    </application>

</manifest>